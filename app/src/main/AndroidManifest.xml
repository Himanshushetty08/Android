<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.database">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_PRIVILEGED_PHONE_STATE"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />


    <application
        android:name=".App"
        android:label="BikeDataService"
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:supportsRtl="true"
        android:persistent="true"
        android:debuggable="true"
        tools:ignore="HardcodedDebugMode,MissingApplicationIcon">

        <!-- FOTA Download Service -->
        <service
            android:name=".service.DatabaseBackgroundService"
            android:enabled="true"
            android:exported="true"
            android:foregroundServiceType="dataSync"
            android:stopWithTask="false"
            tools:ignore="ForegroundServicePermission" />

        <!-- NEW: Dedicated MQTT Upload Service (AIDL) Service -->
        <service
            android:name=".service.MqttUploadService"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.ultraviolette.uvmqtt.MQTT_FILE_SERVICE" />
            </intent-filter>
        </service>

        <!-- Other services -->
        <service
            android:name=".service.UploadJobService"
            android:enabled="true"
            android:exported="false"
            android:permission="android.permission.BIND_JOB_SERVICE"
            tools:ignore="Instantiatable" />

        <service
            android:name=".service.FileQueueManager"
            android:enabled="true"
            android:exported="false"
            tools:ignore="Instantiatable" />

        <service
            android:name=".service.CloudDownloader"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync"
            tools:ignore="Instantiatable" />

        <service
            android:name=".service.CloudDownloadManager"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync"
            tools:ignore="Instantiatable" />

        <!--— UploadManager is NOT a Service -->
        <!-- <service android:name=".service.UploadManager" ... /> -->

        <!-- Emergency Upload Receiver -->
        <receiver
            android:name=".service.EmergencyUploadReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter android:priority="1000">
                <action android:name="com.ultraviolette.ACTION_EMERGENCY_UPLOAD" />
            </intent-filter>
        </receiver>

        <!-- Boot Receiver -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:enabled="true"
            android:exported="true"
            android:directBootAware="true">
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>